***Script for transferring objects from one AWS S3 to another Account of AWS S3 with Different credentials for both***

- Add the Source and Bucket credentials in the `downloadAssets.js`.
- Add the Array of keys in the assetKeys in the `downloadAssets.js`.
- Run the script and it will tranfer all the assets from one AWS account bucket to another proper logging and error Handling.

